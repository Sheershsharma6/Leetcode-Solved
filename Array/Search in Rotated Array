class Solution:
    def search(self, nums: List[int], target: int) -> int:
        for i, num in enumerate(nums):  # Use enumerate to get both index and value
            if num == target:
                return i
        return -1 
